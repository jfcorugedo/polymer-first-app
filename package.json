{
  "scripts": {
    "init": "bower init",
    "bower_install": "bower install --save",
    "start": "polymer serve"
  },
  "engines": {
    "node": ">=0.10.0"
  },
  "devDependencies": {
    "bower": "^1.7.9",
    "polymer-cli": "^0.11.1"
  }
}
